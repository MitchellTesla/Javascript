Example
Use open() to open a new window, and close() to close the new window:

Copy
let myWindow;

function openWin() {
  myWindow = window.open("", "myWindow", "width=200,height=100");
  myWindow.document.write("<p>This is 'myWindow'</p>");
}

function closeWin() {
  myWindow.close();
}

Description
The close() method closes the current window.

This method is often used together with the open() method.

Browser Compatibility
Chrome.png	Node.png	Firefox.png	Safari.png	Edge.png	Opera.png
close()	Yes	Yes	Yes	Yes	Yes	Yes
Syntax
Copy
window.close()
Parameters
None

Return Value
No return value

More Examples
Open "www.demo2s.com" in a new window, and use close() to close the window:

Copy
let myWindow;

function openWin() {
  myWindow = window.open("https://www.demo2s.com", 
                          "_blank", "width=500, height=500");
}

function closeWin() {
  myWindow.close();
}
